import React from "react";

const SocialIcon = ({ className }) => {
  return (
    <a href=" " className="social w-16  social-dark ">
      <i className={`${className} fab`}></i>
    </a>
  );
};

export default SocialIcon;
